# Direct third-party dependencies of ctf_gym.
#
# ctf_gym also imports `dojo` (aira-evo), which is NOT listed here because it is
# installed from the OpenMLE-Evo project. Install both into the SAME environment:
#
#   pip install -r OpenMLE-Evo/requirements.txt
#   pip install -r ctf_gym/requirements.txt
#
# The environment must be Python >=3.11,<3.13 (OpenMLE-Evo's requires-python).

fastapi>=0.110
uvicorn>=0.29
requests>=2.31
